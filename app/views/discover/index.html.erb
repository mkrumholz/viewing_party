<%= button_to "Discover Top 40 Movies" %>

<%= form_with url: search_path, method: :get, local: true do |f| %>
  <%= f.label 'Search by title' %>
  <%= f.text_field :title %>
  <%= f.submit 'Search' %>
<% end %>

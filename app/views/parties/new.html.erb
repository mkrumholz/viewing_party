<h1>Viewing Party Details</h1>
<%= form_with model: @party, local: true do |f| %>
  <%= f.label :title, "Movie Title" %><br><br>
  <%= f.text_field :duration, value: @title, :disabled => true %><br><br>

  <%= f.label :duration, "Duration of Party(min)" %><br>
  <%= f.text_field :duration, placeholder: @duration, min: @duration %><br><br>

  <%= f.label :day, "Day" %><br>
  <%= f.date_field :day %><br><br>

  <%= f.label :start_time, "Start Time" %><br>
  <%= f.time_field :start_time %><br><br>

  <%= f.label :include, "Include" %><br><br>
  <% if current_user.friends.present? %>
    <% current_user.friends.each do |friend| %>
      <%= f.check_box friend.username %>
      <%= f.label friend.username %><br>
    <% end %>
  <% else %>
    <p> You currently have no friends to watch with </p>
  <% end %><br>

  <%= f.submit 'Create Party' %><br>
<% end %>
